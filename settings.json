{
    "files.insertFinalNewline": true, // Good for git
    "editor.snippetSuggestions": "top", // Show snippets first in autocomplete

    ////// Optional configs //////

    // Ctrl/Cmd+Leftclick for mutlicursors (Alt/Opt by default)
    // "editor.multiCursorModifier": "ctrlCmd",

    // Disable current line highlight
    // "editor.renderLineHighlight": "none",

    // Disable minimap
    // "editor.minimap.enabled": false,

    // Disable tab/spaces detection
    // "editor.detectIndentation": false,

    // Disable autoformat on paste
    // "editor.formatOnPaste": false,

    // Change python executable path
    // "python.defaultInterpreterPath": "/bin/python",

    ///// CPP ////

    // Disable builtin intelli Sense
    // "C_Cpp.intelliSenseEngine": "Disabled",
    // "C_Cpp.autocomplete": "Disabled",  // So you don't get autocomplete from both extensions.
    // "C_Cpp.errorSquiggles": "Disabled", // So you don't get error squiggles from both extensions (clangd's seem to be more reliable anyway).

    ////// EXTENSIONS //////

    // Move by symbol
    "gotoNextPreviousMember.symbolKinds": ["class", "enum", "event", "function", "interface", "method", "module", "namespace"],

    // Find => Jump
    // "findJump.dimWhenActive": true,

    ////////////////////////////////////////////////////////////////

}
